# Step 9 Group 3: Student Home Reliability

- Resolves a student display name from supported Campus One/Supabase metadata or the linked profile.
- Uses the neutral `Welcome back` heading when no trustworthy name exists.
- Keeps Events and Updates loading and error states independent.
- Replaces remaining student-home `Club Services` loading copy with `OneClub`.
- Preserves all existing membership, dues, event, announcement, and authentication API contracts.

The display-name regression suite passed 4 tests and TypeScript passed before this checkpoint was committed.
